<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TicketCard" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
            <!-- Defining that new color in ColorPrimaryDark -->
            <item name="colorPrimaryDark">@color/white</item>
    </style>

    <style name="Theme.TicketCard" parent="Base.Theme.TicketCard" />

    <style name="CardGradient." parent="CardView">
        <item name="android:background">@drawable/card_gradient</item>
    </style>
    <style name="overlay." parent="CardView">
        <item name="android:background">@drawable/gradient_overlay_ticket_card</item>
    </style>
</resources>